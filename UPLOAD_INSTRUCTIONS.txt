TESLA OS 27 — VERSION 27.0.1 — BUILD 27A101 — HOTFIX 10 LIGHT SHELL + DEPLOYMENT CACHE FIX

1. Back up the current application tree, persistent database/instance data and uploads before replacing application files.
2. Upload/extract the COMPLETE Hotfix 10 Light Shell package into the application repository root, preserving paths and overwriting matching application/static/template files.
3. If the upload interface has a 100-file limit, upload PART_01_100_FILES first and PART_02_86_FILES second into the exact same application root. Neither split archive is standalone.
4. Preserve DATABASE_URL / PostgreSQL data, SECRET_KEY, LIVENZA_VAULT_MASTER_KEY, INTEGRATION_ENCRYPTION_KEY and all other production environment variables.
5. Preserve all historical migration files and persistent business uploads. Do not reset production data for this shell/performance release.
6. Deploy/restart the web service after files are replaced.
7. BEFORE evaluating the UI, open `/version` and confirm asset revision `27A101-H10L-20260827D`, then verify response header `X-Livenza-Revision` has the same value.
8. In DevTools → Network, Home must request `home_light.css?rev=27A101-H10L-20260827D` and `home_light.js?rev=27A101-H10L-20260827D`. If not, the host/CDN is still serving an older deployment.
9. Confirm Home visually matches the packaged Hotfix 10 preview: Livenza.life scenic wallpaper, seven curated colorful Dock shortcuts, lower-right Companion and no permanent widget column.
10. Open System Settings → Desktop & Dock and confirm the bright refined Settings layout with distinct Dock Size, Magnification and Auto-Hide rows.
11. Run every acceptance check in VERIFY_DEPLOY.txt before accepting the deployment.

A manual browser cache clear should not be required for this release because static CSS/JS URLs carry the new Hotfix revision and the default wallpaper uses a revision-specific filename.


REVISION B PACKAGE
Use only `Tesla_OS_27_v27.0.1_Build_27A101_COMPLETE_HOTFIX10_LIGHT_SHELL_DEPLOYMENT_SURVIVAL_REV_B.zip` or its two split archives. Revision B hardens image/logo/favicon/PWA caching in addition to CSS/JS cache busting.
