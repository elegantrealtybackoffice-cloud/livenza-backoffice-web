LIVENZA WEB 1.5.14 — DIRECT ROOT UPLOAD

1. Extract this ZIP.
2. Upload its contents directly to the root of the existing repository, preserving templates/, static/, migrations/ and windows-kiosk/.
3. No new database migration is required for Web 1.5.14; keep the Web 1.5.12 migration applied.
4. Keep OPENAI_API_KEY configured to enable AI-styled personal avatars. Optional: set OPENAI_AVATAR_MODEL and OPENAI_AVATAR_QUALITY.
5. The avatar pipeline now supports HEIC/HEIF through the added pillow-heif dependency; redeploy dependencies from requirements.txt.
6. Deploy with the supplied Dockerfile/render.yaml and confirm /version returns Web 1.5.14.
7. Verify the Full Screen button, 360° lifestyle background, camera/selfie capture, and angled/candid avatar generation before production use.

The Windows scripts are setup helpers. Full operating-system restriction requires Microsoft Assigned Access as described in windows-kiosk/README_WINDOWS_KIOSK.md.
