TESLA OS 27 — VERSION 27.0.1 — BUILD 27A101 — HOTFIX 10 LIVENZA BRAND SYSTEM + RUNTIME RELIABILITY

1. Back up the current application tree, persistent database/instance data and uploads before replacing application files.
2. Upload/extract the COMPLETE Hotfix 10 package into the application repository root, preserving paths and overwriting matching application/static/template files.
3. If the upload interface has a 100-file limit, upload PART_01_100_FILES first and PART_02 second into the exact same application root. Neither split archive is a standalone application.
4. Preserve DATABASE_URL / PostgreSQL data, SECRET_KEY, LIVENZA_VAULT_MASTER_KEY, INTEGRATION_ENCRYPTION_KEY and all other production environment variables.
5. Preserve all historical migration files and persistent business uploads. Do not reset production data for this UI/brand release.
6. Deploy with the supplied Dockerfile/render.yaml and confirm `/version` still reports Tesla OS 27, Version 27.0.1 and Build 27A101.
7. Hard-refresh once after deployment so browsers/PWA caches receive the new Hotfix 10 brand icons, CSS and JavaScript.
8. Confirm the new Livenza logo system appears everywhere appropriate: wordmark in the Home menu bar, tagline lockup on login/kiosk/formal documents and `.life` mark in compact system/app-icon contexts.
9. Run every acceptance check in VERIFY_DEPLOY.txt before accepting the deployment.
